{
  "scripts": {
    "start": "npm run front && npm run back && npm run app",
    "front": "cd client && start npm start && cd ..",
    "back": "cd server && start npm run dev && cd..",
    "app": "cd client && npm run electron && cd.."
  },
  "dependencies": {
    "git": "^0.1.5"
  }
}
